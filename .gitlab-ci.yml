include:
  - project: 'platform-one/big-bang/pipeline-templates/pipeline-templates'
    ref: vault-passthrough
    file: '/pipelines/bigbang.yaml'
variables:
  PIPELINE_REPO_BRANCH: vault-passthrough
  # DEBUG_ENABLED: "true"
